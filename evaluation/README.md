# Evaluation Infrastructure

This directory contains RAG evaluation benchmarks, test datasets, and automated quality metrics (groundedness, citation accuracy, answer relevance) for the AI Tutor and retrieval subsystem.

## Structure
- `datasets/`: Ground truth evaluation datasets.
- `evaluators/`: Metrics calculators (e.g. faithfulness, answer correctness).
- `run_eval.py`: Evaluation CLI entrypoint.
